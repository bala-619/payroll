import 'package:flutter/material.dart';

class SettingsModel{
  String title;
  String img;
  Color selectColor;
  Color unSelectColor;
  SettingsModel({required this.title,required this.img,required this.selectColor,required this.unSelectColor});
}